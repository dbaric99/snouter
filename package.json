{
  "devDependencies": {
    "turbo": "^1.10.6"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/main",
    "start:dev": "nest start --watch"
  }
}
